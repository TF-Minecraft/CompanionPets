name: CompanionPets
main: net.tfminecraft.companionpets.PetsPlugin
version: ${project.version}
api-version: "1.21.10"
description: Pets you hatch, care for, and train. Each one keeps its needs, favorite toy, and tricks.

softdepend:
  - MythicMobs
  - ModelEngine
  - ItemsAdder
  - MMOItems
